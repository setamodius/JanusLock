services:
  janus:
    image: janus:1.0.0
    container_name: janus
    restart: unless-stopped
    ports:
      - "9494:3000"
    environment:
      - MQTT_URL=${MQTT_URL}
      - MQTT_USER=${MQTT_USER}
      - MQTT_PASS=${MQTT_PASS}
